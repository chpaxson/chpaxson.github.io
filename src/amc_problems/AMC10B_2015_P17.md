<!-- metadata -->
2015 AMC10B
Problem 17
answer = 2


# Problem
The centers of the faces of the right rectangular prism shown are joined to create an octahedron.  The prism has dimensions $3\times4\times5$.  What is the volume of this octahedron?

answers = $\frac{75}{12}$, $10$, $12$, $10\sqrt{2}$, $15$


# Solution

```interactive-html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { background: #1a1a2e; overflow: hidden; font-family: sans-serif; }
  canvas { display: block; }
  #labels { position: absolute; top: 0; left: 0; pointer-events: none; }
  .edge-label {
    position: absolute;
    color: #fff;
    font-size: 13px;
    font-weight: 600;
    background: rgba(0,0,0,0.55);
    padding: 2px 6px;
    border-radius: 4px;
    transform: translate(-50%, -50%);
    white-space: nowrap;
  }
  #hint { position: absolute; bottom: 10px; left: 50%; transform: translateX(-50%);
    color: rgba(255,255,255,0.45); font-size: 12px; pointer-events: none; }
</style>
</head>
<body>
<div id="labels"></div>
<div id="hint">Drag to rotate · Scroll to zoom</div>
<script type="importmap">
  { "imports": { "three": "https://cdn.jsdelivr.net/npm/three@0.163.0/build/three.module.js",
                 "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.163.0/examples/jsm/" } }
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

// ── Scene ────────────────────────────────────────────────────────────────────
const W = window.innerWidth, H = window.innerHeight;
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(W, H);
renderer.setPixelRatio(devicePixelRatio);
document.body.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);

const camera = new THREE.PerspectiveCamera(45, W/H, 0.1, 200);
camera.position.set(9, 7, 11);
camera.lookAt(0, 0, 0);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;

scene.add(new THREE.AmbientLight(0xffffff, 0.6));
const dl = new THREE.DirectionalLight(0xffffff, 0.9);
dl.position.set(8, 12, 8);
scene.add(dl);

// ── Dimensions ───────────────────────────────────────────────────────────────
const W3 = 3, H3 = 4, D3 = 5;   // prism dimensions
const hx = W3/2, hy = H3/2, hz = D3/2;

// ── Prism (wireframe) ─────────────────────────────────────────────────────────
const prismGeo = new THREE.BoxGeometry(W3, H3, D3);
const prismEdges = new THREE.EdgesGeometry(prismGeo);
const prismMesh = new THREE.LineSegments(
  prismEdges,
  new THREE.LineBasicMaterial({ color: 0x5588bb, transparent: true, opacity: 0.45 })
);
scene.add(prismMesh);

// Transparent prism faces
const prismFaceMesh = new THREE.Mesh(
  prismGeo,
  new THREE.MeshStandardMaterial({ color: 0x3366aa, transparent: true, opacity: 0.08, side: THREE.DoubleSide })
);
scene.add(prismFaceMesh);

// ── Octahedron vertices = face centers of prism ───────────────────────────────
// +x, -x, +y, -y, +z, -z face centers
const verts = [
  new THREE.Vector3( hx,  0,  0),  // 0  right
  new THREE.Vector3(-hx,  0,  0),  // 1  left
  new THREE.Vector3(  0, hy,  0),  // 2  top
  new THREE.Vector3(  0,-hy,  0),  // 3  bottom
  new THREE.Vector3(  0,  0, hz),  // 4  front
  new THREE.Vector3(  0,  0,-hz),  // 5  back
];

// All 12 edges of the octahedron
// Each equatorial vertex connects to all 4 "belt" vertices; poles to belt.
// The 6-vertex octahedron edges: every vertex connects to every other except its opposite.
// Pairs (i,j) where i < j and they are NOT antipodal (0↔1, 2↔3, 4↔5)
const octEdges = [];
const antipodal = new Set(['0-1','1-0','2-3','3-2','4-5','5-4']);
for (let i=0;i<6;i++) for (let j=i+1;j<6;j++) {
  if (!antipodal.has(`${i}-${j}`)) octEdges.push([i,j]);
}

// Draw octahedron edges (uniform gold)
octEdges.forEach(([i,j]) => {
  const geo = new THREE.BufferGeometry().setFromPoints([verts[i], verts[j]]);
  scene.add(new THREE.Line(geo, new THREE.LineBasicMaterial({ color: 0xffd700, linewidth: 2 })));
});

// Octahedron filled faces (semi-transparent)
const octPositions = [];
// 8 triangular faces — for each octant pick the 3 vertices not in that octant's opposite
// Faces of a regular-ish octahedron with vertices ±x,±y,±z:
const octFaces = [
  [0,2,4],[0,2,5],[0,3,4],[0,3,5],
  [1,2,4],[1,2,5],[1,3,4],[1,3,5],
];
octFaces.forEach(([a,b,c]) => {
  octPositions.push(...verts[a].toArray(), ...verts[b].toArray(), ...verts[c].toArray());
});
const octGeo = new THREE.BufferGeometry();
octGeo.setAttribute('position', new THREE.Float32BufferAttribute(octPositions, 3));
octGeo.computeVertexNormals();
const octMesh = new THREE.Mesh(
  octGeo,
  new THREE.MeshStandardMaterial({ color: 0xffd700, transparent: true, opacity: 0.18, side: THREE.DoubleSide })
);
scene.add(octMesh);

// Vertex spheres
verts.forEach((v,i) => {
  const s = new THREE.Mesh(
    new THREE.SphereGeometry(0.10, 16, 16),
    new THREE.MeshStandardMaterial({ color: 0xffd700, emissive: 0xffd700, emissiveIntensity: 0.3 })
  );
  s.position.copy(v);
  scene.add(s);
});

// ── Dashed lines from each vertex to center + labels ────────────────────────
const labelsDiv = document.getElementById('labels');
const labelDefs = [];
const origin = new THREE.Vector3(0, 0, 0);

// One dashed line per unique axis direction (show one of each pair ±x, ±y, ±z)
const radiiDefs = [
  { v: verts[0], color: 0xe05c5c, text: '3/2' },  // +x
  { v: verts[2], color: 0x58d68d, text: '4/2'   },  // +y
  { v: verts[4], color: 0x5b9cf6, text: '5/2' },  // +z
];

// Draw all 6 dashed spokes but only label 3
verts.forEach((v) => {
  const geo = new THREE.BufferGeometry().setFromPoints([origin, v]);
  const mat = new THREE.LineDashedMaterial({
    color: 0xffffff, dashSize: 0.18, gapSize: 0.12, transparent: true, opacity: 0.55,
    depthTest: false,
  });
  const line = new THREE.Line(geo, mat);
  line.computeLineDistances();
  scene.add(line);
});

// Labels at midpoints of the three representative spokes
radiiDefs.forEach(({ v, color, text }) => {
  const mid = v.clone().multiplyScalar(0.5);
  const el = document.createElement('div');
  el.className = 'edge-label';
  el.style.color = '#' + color.toString(16).padStart(6,'0');
  el.textContent = text;
  labelsDiv.appendChild(el);
  labelDefs.push({ el, mid });
});

// ── Prism dimension labels on axes ───────────────────────────────────────────
const dimDefs = [
  { pos: new THREE.Vector3(0, -hy-0.3, -hz-0.35), text: '3' },
  { pos: new THREE.Vector3(-hx-0.4, 0, -hz-0.35), text: '4' },
  { pos: new THREE.Vector3(-hx-0.4, -hy-0.3, 0),  text: '5' },
];
dimDefs.forEach(d => {
  const el = document.createElement('div');
  el.className = 'edge-label';
  el.style.color = '#88aacc';
  el.style.fontSize = '11px';
  el.textContent = d.text;
  labelsDiv.appendChild(el);
  labelDefs.push({ el, mid: d.pos });
});

// ── Project helper ────────────────────────────────────────────────────────────
const tmp = new THREE.Vector3();
function project(pos) {
  tmp.copy(pos).project(camera);
  return {
    x: ( tmp.x * 0.5 + 0.5) * renderer.domElement.clientWidth,
    y: (-tmp.y * 0.5 + 0.5) * renderer.domElement.clientHeight,
  };
}

// ── Animate ───────────────────────────────────────────────────────────────────
function animate() {
  requestAnimationFrame(animate);
  controls.update();
  // Update label positions
  labelDefs.forEach(({ el, mid }) => {
    const p = project(mid);
    el.style.left = p.x + 'px';
    el.style.top  = p.y + 'px';
  });
  renderer.render(scene, camera);
}
animate();

// ── Resize ────────────────────────────────────────────────────────────────────
window.addEventListener('resize', () => {
  const w = window.innerWidth, h = window.innerHeight;
  camera.aspect = w/h;
  camera.updateProjectionMatrix();
  renderer.setSize(w, h);
});

// ── iframe height reporting ───────────────────────────────────────────────────
window.parent.postMessage({ iframeHeight: 520 }, '*');
</script>
</body>
</html>
```

If we split the rectangular prism in half on each axis, we will have 8 "corners" to the prism.  Splitting the octahedron up along the same planes will give us 8 identical right triangular pyramids, with edge 
lengths half of the rectangle's side lengths.



The volume of a pyramid is $\frac{1}{3} \cdot B \cdot h$, where $B$ is base area and $h$ is height.

Therefore the volume is
$$V=8\cdot \frac{1}{3} \left(\frac{1}{2} \cdot \frac{3}{2}\cdot\frac{4}{2}\cdot\frac{5}{2}\right)=\boxed{10}$$  