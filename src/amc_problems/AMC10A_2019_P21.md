<!-- metadata -->
2019 AMC10A
Problem 21
answer = 3


# Problem
A sphere with center $O$ has radius $6$.  A triangle with sides of length $15$, $15$, and $24$ is situated in space so that each of its sides is tangent to the sphere.  What is the distance between $O$ and the plane determined by the triangle?

answers = $2\sqrt{3}$, $4$, $3\sqrt{2}$, $2\sqrt{5}$, $5$


# Solution

```interactive-html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<style>
  * { margin:0; padding:0; box-sizing:border-box; }
  html, body { width:100%; height:100%; background:#1a1a2e; overflow:hidden; font-family:sans-serif; }
  canvas { display:block; }
  #labels { position:absolute; top:0; left:0; pointer-events:none; }
  .lbl { position:absolute; font-size:12px; font-weight:600; background:rgba(0,0,0,0.65);
         padding:2px 6px; border-radius:4px; transform:translate(-50%,-50%); white-space:nowrap; }
  #hint { position:absolute; bottom:8px; left:50%; transform:translateX(-50%);
          font-size:11px; color:rgba(255,255,255,0.35); pointer-events:none; }
</style>
</head>
<body>
<div id="labels"></div>
<div id="hint">Drag to rotate · Scroll to zoom</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.163.0/build/three.module.js",
            "three/addons/":"https://cdn.jsdelivr.net/npm/three@0.163.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

// ── Geometry of the triangle ──────────────────────────────────────────────────
// Isosceles triangle: sides 15, 15, 24. Place base along x-axis, centred at origin.
// Base half = 12, height = sqrt(15²−12²) = sqrt(81) = 9
const BASE = 24, LEG = 15;
const halfBase = 12, triHeight = 9; // 9 = sqrt(225-144)

// Vertices (in the plane y=0):
const A = new THREE.Vector3(-halfBase, 0, 0);
const B = new THREE.Vector3( halfBase, 0, 0);
const C = new THREE.Vector3(0, 0, -triHeight); // apex "into" screen

// Incenter (for isosceles, lies on altitude from apex, at distance r from base)
// s=27, area=108, r=4
const inr = 4;
// Incenter x=0 by symmetry; z: distance from base line (y=0, z=0... base is y=z=0 line)
// The incenter is at z = -r_offset where r_offset satisfies tangency to base.
// Actually: incenter coords = (a·xA + b·xB + c·xC)/(a+b+c)
// a=|BC|=15, b=|AC|=15, c=|AB|=24
// ix = (15*(-12)+15*12+24*0)/54 = 0
// iz = (15*0 + 15*0 + 24*(-9))/54 = -216/54 = -4
const incenter = new THREE.Vector3(0, 0, -4);

// Sphere: radius 6, center O at (0, d, -4) where d = 2√5
const sphereR = 6;
const d = 2 * Math.sqrt(5); // ≈ 4.47
const O = new THREE.Vector3(0, d, -4);

// ── Renderer / scene ──────────────────────────────────────────────────────────
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(devicePixelRatio);
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);
scene.add(new THREE.AmbientLight(0xffffff, 0.55));
const dl = new THREE.DirectionalLight(0xffffff, 0.9);
dl.position.set(6, 10, 8); scene.add(dl);

const camera = new THREE.PerspectiveCamera(38, window.innerWidth / window.innerHeight, 0.1, 300);
camera.position.set(20, 14, 18);
camera.lookAt(0, d / 2, -4);

const controls = new OrbitControls(camera, renderer.domElement);
controls.target.set(0, d / 2, -4);
controls.enableDamping = true;

// ── Triangle (filled, transparent) ───────────────────────────────────────────
{
  const geo = new THREE.BufferGeometry();
  geo.setAttribute('position', new THREE.Float32BufferAttribute([
    A.x,A.y,A.z, B.x,B.y,B.z, C.x,C.y,C.z
  ], 3));
  geo.setIndex([0,1,2]);
  geo.computeVertexNormals();
  scene.add(new THREE.Mesh(geo, new THREE.MeshStandardMaterial({
    color: 0x4a90d9, side: THREE.DoubleSide, transparent: true, opacity: 0.22, depthWrite: false
  })));
  // Edges
  const edgePts = [A, B, B, C, C, A];
  const edgeArr = [];
  edgePts.forEach(v => { edgeArr.push(v.x, v.y, v.z); });
  const eg = new THREE.BufferGeometry();
  eg.setAttribute('position', new THREE.Float32BufferAttribute(edgeArr, 3));
  scene.add(new THREE.LineSegments(eg, new THREE.LineBasicMaterial({ color: 0x74b9ff, linewidth: 2 })));
}

// ── Sphere ────────────────────────────────────────────────────────────────────
{
  const mat = new THREE.MeshStandardMaterial({ color: 0xf39c12, transparent: true, opacity: 0.35,
    roughness: 0.2, metalness: 0.1, depthWrite: false });
  const sphere = new THREE.Mesh(new THREE.SphereGeometry(sphereR, 48, 48), mat);
  sphere.position.copy(O);
  scene.add(sphere);
  // Wireframe ring
  const wMat = new THREE.MeshBasicMaterial({ color: 0xf39c12, wireframe: true, transparent: true, opacity: 0.15 });
  const wire = new THREE.Mesh(new THREE.SphereGeometry(sphereR, 16, 8), wMat);
  wire.position.copy(O);
  scene.add(wire);
}

// ── Helper: dashed line ───────────────────────────────────────────────────────
function dash(a, b, color, opacity = 0.9) {
  const geo = new THREE.BufferGeometry().setFromPoints([a, b]);
  const mat = new THREE.LineDashedMaterial({ color, dashSize: 0.3, gapSize: 0.15,
    transparent: true, opacity, depthTest: false });
  const l = new THREE.Line(geo, mat); l.computeLineDistances(); scene.add(l);
}

// Vertical drop from O to incenter (the distance d = 2√5)
const Ofoot = new THREE.Vector3(0, 0, -4); // foot on triangle plane
dash(O, Ofoot, 0xc39bd3);

// Right-angle marker at foot (small square)
{
  const s = 0.45;
  const sq = new THREE.BufferGeometry().setFromPoints([
    new THREE.Vector3(s, 0, -4), new THREE.Vector3(s, 0, -4+s),
    new THREE.Vector3(0, 0, -4+s)
  ]);
  scene.add(new THREE.Line(sq, new THREE.LineBasicMaterial({ color: 0xffffff, transparent: true, opacity: 0.5 })));
}

// Line from incenter to nearest point on base (shows inradius r=4)
const tangentPtBase = new THREE.Vector3(0, 0, 0); // foot of perp from incenter to base
dash(Ofoot, tangentPtBase, 0x5b9cf6);

// Line from O to that tangent point on base (= sphere radius = 6)
dash(O, tangentPtBase, 0xf39c12);

// Center O dot
{
  const dot = new THREE.Mesh(new THREE.SphereGeometry(0.18, 16, 16),
    new THREE.MeshBasicMaterial({ color: 0xffffff }));
  dot.position.copy(O); scene.add(dot);
}
// Incenter dot
{
  const dot = new THREE.Mesh(new THREE.SphereGeometry(0.18, 16, 16),
    new THREE.MeshBasicMaterial({ color: 0xc39bd3 }));
  dot.position.copy(Ofoot); scene.add(dot);
}

// ── Labels ────────────────────────────────────────────────────────────────────
const labelsDiv = document.getElementById('labels');
const labelDefs = [];
function addLabel(text, color, pos) {
  const el = document.createElement('div');
  el.className = 'lbl'; el.style.color = color; el.textContent = text;
  labelsDiv.appendChild(el); labelDefs.push({ el, pos });
}
// d = 2√5 (vertical)
addLabel('d = 2√5', '#c39bd3', new THREE.Vector3(0.9, d / 2, -4));
// r = 4 (inradius, blue)
addLabel('r = 4', '#5b9cf6', new THREE.Vector3(0.9, 0, -2));
// radius = 6 (orange hypotenuse)
addLabel('R = 6', '#f39c12', new THREE.Vector3(3.5, d / 2 + 0.5, -1));
// Triangle labels
addLabel('24', '#74b9ff', new THREE.Vector3(0, -0.7, 0));
addLabel('15', '#74b9ff', new THREE.Vector3(7, -0.7, -4.5));
addLabel('15', '#74b9ff', new THREE.Vector3(-7, -0.7, -4.5));
addLabel('O', '#ffffff', new THREE.Vector3(0, d + 0.6, -4));

// ── Animate ───────────────────────────────────────────────────────────────────
const tmp = new THREE.Vector3();
function project(p) {
  tmp.copy(p).project(camera);
  return { x: (tmp.x+1)/2*window.innerWidth, y: (1-(tmp.y+1)/2)*window.innerHeight };
}
function animate() {
  requestAnimationFrame(animate);
  controls.update();
  labelDefs.forEach(({ el, pos }) => {
    const p = project(pos); el.style.left = p.x+'px'; el.style.top = p.y+'px';
  });
  renderer.render(scene, camera);
}
animate();

window.addEventListener('resize', () => {
  renderer.setSize(window.innerWidth, window.innerHeight);
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
});

window.parent.postMessage({ iframeHeight: 500 }, '*');
</script>
</body>
</html>
```

**Step 1: Find the inradius of the triangle.**

The triangle has sides $a = 15$, $b = 15$, $c = 24$. Using Heron's formula:
$$s = \frac{15+15+24}{2} = 27, \qquad A = \sqrt{27 \cdot 12 \cdot 12 \cdot 3} = \sqrt{11664} = 108$$
$$r = \frac{A}{s} = \frac{108}{27} = 4$$

**Step 2: Locate the sphere center.**

Let $d$ be the distance from $O$ to the plane of the triangle, and let $P$ be the foot of the perpendicular from $O$ to that plane. Since the sphere is tangent to all three sides, $P$ is equidistant from all three sides in the plane — so $P$ must be the **incenter**, at distance $r = 4$ from each side.

**Step 3: Apply the 3D distance formula.**

The distance from $O$ to each side (a line in 3D) equals:
$$\sqrt{d^2 + r^2} = R \implies \sqrt{d^2 + 16} = 6 \implies d^2 = 20 \implies d = \boxed{2\sqrt{5}}$$
