<!-- metadata -->
2013 AMC10A
Problem 22
answer = 1


# Problem
Six spheres of radius $1$ are positioned so that their centers are at the vertices of a regular hexagon of side length 2.  The six spheres are internally tangent to a larger sphere whose center is the center of the hexagon.  An eighth sphere is externally tangent to the six smaller spheres and internally tangent to the larger sphere.  What is the radius of this eighth sphere?

answers = $\sqrt{2}$, $3/2$, $5/3$, $\sqrt{3}$, $2$


# Solution

```interactive-html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<style>
  * { margin:0; padding:0; box-sizing:border-box; }
  html, body { width:100%; height:100%; background:#1a1a2e; overflow:hidden; font-family:sans-serif; }
  canvas { display:block; }
  #labels { position:absolute; top:0; left:0; pointer-events:none; }
  .lbl { position:absolute; font-size:12px; font-weight:600; background:rgba(0,0,0,0.65);
         padding:2px 6px; border-radius:4px; transform:translate(-50%,-50%); white-space:nowrap; }
  #hint { position:absolute; bottom:8px; left:50%; transform:translateX(-50%);
          font-size:11px; color:rgba(255,255,255,0.35); pointer-events:none; }
</style>
</head>
<body>
<div id="labels"></div>
<div id="hint">Drag to rotate · Scroll to zoom</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.163.0/build/three.module.js",
            "three/addons/":"https://cdn.jsdelivr.net/npm/three@0.163.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const R_LARGE = 3;       // large sphere radius
const r_small = 1;       // small sphere radius
const r_eighth = 1.5;    // eighth sphere radius (3/2)
const hexR = 2;          // distance from center to small sphere centers
const h = R_LARGE - r_eighth; // = 1.5, height of eighth sphere center above plane

// ── Renderer / scene ──────────────────────────────────────────────────────────
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(devicePixelRatio);
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);
scene.add(new THREE.AmbientLight(0xffffff, 0.5));
const dl = new THREE.DirectionalLight(0xffffff, 0.9);
dl.position.set(5, 10, 8); scene.add(dl);

const camera = new THREE.PerspectiveCamera(38, window.innerWidth / window.innerHeight, 0.1, 200);
camera.position.set(8, 6, 10);
camera.lookAt(0, h / 2, 0);

const controls = new OrbitControls(camera, renderer.domElement);
controls.target.set(0, h / 2, 0);
controls.enableDamping = true;

// ── Large sphere (transparent) ────────────────────────────────────────────────
const largeMat = new THREE.MeshStandardMaterial({
  color: 0x2a6496, transparent: true, opacity: 0.12,
  side: THREE.DoubleSide, depthWrite: false
});
const largeSphere = new THREE.Mesh(new THREE.SphereGeometry(R_LARGE, 48, 48), largeMat);
scene.add(largeSphere);
// Wireframe overlay
const lwMat = new THREE.MeshBasicMaterial({ color: 0x4a90d9, wireframe: true, transparent: true, opacity: 0.08 });
scene.add(new THREE.Mesh(new THREE.SphereGeometry(R_LARGE, 24, 12), lwMat));

// ── 6 small spheres at hexagon vertices ──────────────────────────────────────
const smallMat = new THREE.MeshStandardMaterial({ color: 0x5b9cf6, roughness: 0.25, metalness: 0.1 });
const smallCenters = [];
for (let i = 0; i < 6; i++) {
  const angle = (Math.PI / 3) * i;
  const cx = hexR * Math.cos(angle);
  const cz = hexR * Math.sin(angle);
  const center = new THREE.Vector3(cx, 0, cz);
  smallCenters.push(center);
  const m = new THREE.Mesh(new THREE.SphereGeometry(r_small, 32, 32), smallMat);
  m.position.copy(center);
  scene.add(m);
}

// ── Eighth sphere (above, orange) ─────────────────────────────────────────────
const eighthMat = new THREE.MeshStandardMaterial({ color: 0xf39c12, roughness: 0.2, metalness: 0.1 });
const eighthSphere = new THREE.Mesh(new THREE.SphereGeometry(r_eighth, 40, 40), eighthMat);
eighthSphere.position.set(0, h, 0);
scene.add(eighthSphere);

// ── Dashed line helper ────────────────────────────────────────────────────────
function dash(a, b, color, opacity = 0.9) {
  const geo = new THREE.BufferGeometry().setFromPoints([a, b]);
  const mat = new THREE.LineDashedMaterial({ color, dashSize: 0.2, gapSize: 0.1,
    transparent: true, opacity, depthTest: false });
  const l = new THREE.Line(geo, mat);
  l.computeLineDistances();
  scene.add(l);
}

// Center of everything
const origin = new THREE.Vector3(0, 0, 0);
const eighthCenter = new THREE.Vector3(0, h, 0);
// Pick one small sphere for annotation
const s0 = smallCenters[0]; // (2, 0, 0)

// Horizontal distance from origin to small sphere center (= 2)
dash(origin, new THREE.Vector3(s0.x, 0, s0.z), 0x74b9ff);

// Vertical from origin to eighth sphere center (= h = 3/2)
dash(origin, eighthCenter, 0xc39bd3);

// From eighth center to small sphere center (= 1 + 3/2 = 5/2)
dash(eighthCenter, s0, 0xf39c12);

// Large sphere radius to small sphere center (= 3)
dash(origin, s0, 0x2ecc71, 0.7);

// ── Hexagon outline connecting small sphere centers ───────────────────────────
{
  const pts = [...smallCenters, smallCenters[0]]; // close the loop
  const hexArr = [];
  pts.forEach(v => { hexArr.push(v.x, v.y, v.z); });
  const hexGeo = new THREE.BufferGeometry();
  hexGeo.setAttribute('position', new THREE.Float32BufferAttribute(hexArr, 3));
  scene.add(new THREE.Line(hexGeo, new THREE.LineBasicMaterial({ color: 0xffffff, transparent: true, opacity: 0.4, depthTest: false })));
}

// Dot at origin
const originDot = new THREE.Mesh(new THREE.SphereGeometry(0.1, 16, 16),
  new THREE.MeshBasicMaterial({ color: 0xffffff }));
scene.add(originDot);

// ── Labels ────────────────────────────────────────────────────────────────────
const labelsDiv = document.getElementById('labels');
const labelDefs = [];
function addLabel(text, color, pos) {
  const el = document.createElement('div');
  el.className = 'lbl'; el.style.color = color; el.textContent = text;
  labelsDiv.appendChild(el);
  labelDefs.push({ el, pos: pos.clone() });
}

addLabel('R = 3', '#2ecc71',
  new THREE.Vector3(s0.x / 2 + 0.3, 0.5, s0.z / 2));
addLabel('h = 3/2', '#c39bd3',
  new THREE.Vector3(0.5, h / 2, 0));
addLabel('r₁+r₈ = 5/2', '#f39c12',
  new THREE.Vector3(s0.x / 2 - 0.5, h / 2 + 0.4, s0.z / 2));
addLabel('2', '#74b9ff',
  new THREE.Vector3(s0.x / 2, -0.5, s0.z / 2));
addLabel('r₈ = 3/2', '#f39c12',
  new THREE.Vector3(0, h + r_eighth + 0.4, 0));
addLabel('r₁ = 1', '#5b9cf6',
  new THREE.Vector3(s0.x + 0.6, 0.6, s0.z));
addLabel('O', '#ffffff', new THREE.Vector3(0.35, 0.2, 0));

// ── Animate ───────────────────────────────────────────────────────────────────
const tmp = new THREE.Vector3();
function project(p) {
  tmp.copy(p).project(camera);
  return { x: (tmp.x + 1) / 2 * window.innerWidth, y: (1 - (tmp.y + 1) / 2) * window.innerHeight };
}
function animate() {
  requestAnimationFrame(animate);
  controls.update();
  labelDefs.forEach(({ el, pos }) => {
    const p = project(pos);
    el.style.left = p.x + 'px';
    el.style.top = p.y + 'px';
  });
  renderer.render(scene, camera);
}
animate();

window.addEventListener('resize', () => {
  renderer.setSize(window.innerWidth, window.innerHeight);
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
});

window.parent.postMessage({ iframeHeight: 500 }, '*');
</script>
</body>
</html>
```

**Step 1: Find the radius of the large sphere.**

The centers of the six small spheres lie at the vertices of a regular hexagon with side length 2. For a regular hexagon, the circumradius equals the side length, so each small sphere's center is **distance 2** from the hexagon's center $O$. Since each small sphere (radius 1) is internally tangent to the large sphere (radius $R$):
$$R - 1 = 2 \implies R = 3$$

**Step 2: Set up coordinates for the eighth sphere.**

By symmetry, the eighth sphere's center lies on the axis perpendicular to the hexagon plane through $O$, at height $h$ above the plane. Let its radius be $r$.

- **Internally tangent to large sphere:** distance from $O$ to eighth center $= R - r$, so $h = 3 - r$.
- **Externally tangent to each small sphere:** distance from eighth center $(0, h, 0)$ to a small center $(2, 0, 0)$ equals $1 + r$:
$$\sqrt{4 + h^2} = 1 + r$$

**Step 3: Solve.**

Substitute $h = 3 - r$:
$$4 + (3-r)^2 = (1+r)^2$$
$$4 + 9 - 6r + r^2 = 1 + 2r + r^2$$
$$13 - 6r = 1 + 2r \implies 12 = 8r \implies r = \boxed{\dfrac{3}{2}}$$
