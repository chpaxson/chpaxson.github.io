<!-- metadata -->
2009 AMC12A
Problem 22
answer = 1


# Problem
A regular octahedron has side length 1. A plane parallel to two of its opposite faces cuts the octahedron into two 
congruent solids. What is the area of the polygon formed by the intersection of the plane and the octahedron?

answers = $\frac{3\sqrt{3}}{8}$, $\frac{\sqrt{3}}{4}$, $\frac{3\sqrt{3}}{4}$, $\frac{3}{4}$, $\frac{\sqrt{3}}{2}$

# Solution

**Setting up coordinates.** Place the regular octahedron with vertices at $(\pm a, 0, 0)$, $(0, \pm a, 0)$, $(0, 0, \pm a)$, where the edge length is $a\sqrt{2}$. For unit edge: $a = \frac{1}{\sqrt{2}}$.

**Identifying the opposite faces.** One pair of opposite (parallel) faces lies in the planes $x+y+z = a$ and $x+y+z = -a$. A plane parallel to both and equidistant from each is $x+y+z = 0$.

**Finding the cross-section.** Label the top-side vertices $A=(a,0,0)$, $B=(0,a,0)$, $C=(0,0,a)$ (with $x+y+z>0$) and the bottom-side vertices $D=(-a,0,0)$, $E=(0,-a,0)$, $F=(0,0,-a)$ (with $x+y+z<0$). The plane $x+y+z=0$ crosses exactly the 6 **belt edges** connecting upper to lower vertices (e.g.\ $AE$, $AF$, $BD$, $BF$, $CD$, $CE$), each at its midpoint.

The 6 midpoints are:
$$M_{AE}=\tfrac{a}{2}(1,-1,0),\; M_{BF}=\tfrac{a}{2}(0,1,-1),\; M_{CD}=\tfrac{a}{2}(-1,0,1),\ldots$$

**Verifying the hexagon is regular.** Each hexagon side is a **midsegment** of one of the lateral triangular faces, so by the midpoint theorem its length equals half the opposite edge of that triangle. For unit edge, each face has edge 1, so:
$$\text{hexagon side } s = \tfrac{1}{2}$$
All six sides are equal and the interior angles are all $120°$ (verified by dot product), confirming a **regular hexagon**.

**Area.**
$$A = \frac{3\sqrt{3}}{2}s^2 = \frac{3\sqrt{3}}{2}\cdot\frac{1}{4} = \boxed{\dfrac{3\sqrt{3}}{8}}$$

The answer is $\textbf{(A)}$.

```interactive-html
<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0d0d1a;overflow:hidden;font-family:system-ui,sans-serif}
canvas{display:block}
#hint{position:absolute;top:12px;left:12px;color:#445;font-size:11px;line-height:1.9}
#ui{
  position:absolute;bottom:16px;left:50%;transform:translateX(-50%);
  background:rgba(10,10,30,.92);border:1px solid rgba(120,140,255,.3);
  border-radius:12px;padding:10px 24px;display:flex;align-items:center;gap:14px
}
#ui label{color:#99a;font-size:12px;white-space:nowrap}
input[type=range]{
  flex:1;-webkit-appearance:none;height:4px;border-radius:2px;width:200px;
  background:linear-gradient(to right,#4a9eff,#9b6bff);cursor:pointer;outline:none
}
input[type=range]::-webkit-slider-thumb{
  -webkit-appearance:none;width:16px;height:16px;border-radius:50%;
  background:#fff;cursor:pointer;box-shadow:0 0 6px rgba(74,158,255,.7)
}
#legend{
  position:absolute;top:12px;right:12px;
  background:rgba(10,10,30,.88);border:1px solid rgba(120,140,255,.25);
  border-radius:10px;padding:10px 16px;font-size:12px;line-height:2.2;color:#aac
}
</style></head><body>
<div id="hint">Drag to orbit · Scroll to zoom</div>
<div id="legend">
  <b style="color:#ccd">Regular Octahedron, edge = 1</b><br>
  <span style="color:#4a9eff">●</span> Upper half &nbsp;
  <span style="color:#ff6644">●</span> Lower half<br>
  <span style="color:#ffcc44">━</span> Hexagonal cross-section<br>
  Side = ½ &nbsp;·&nbsp; Area = <b style="color:#7cf">3√3/8</b>
</div>
<div id="ui">
  <label>Together</label>
  <input type="range" id="sl" min="0" max="100" value="0"/>
  <label>Separated</label>
</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js","three/addons/":"https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const a = 1/Math.sqrt(2); // so edge = a*sqrt(2) = 1
const GOLD = 0xffcc44;

// 6 octahedron vertices
const V = {
  A: new THREE.Vector3( a,  0,  0),
  B: new THREE.Vector3( 0,  a,  0),
  C: new THREE.Vector3( 0,  0,  a),
  D: new THREE.Vector3(-a,  0,  0),
  E: new THREE.Vector3( 0, -a,  0),
  F: new THREE.Vector3( 0,  0, -a),
};

// Midpoints of belt edges (hexagon vertices)
function mid(p, q) { return p.clone().add(q).multiplyScalar(0.5); }
const H = [
  mid(V.A, V.E),  // MAE
  mid(V.A, V.F),  // MAF
  mid(V.B, V.F),  // MBF
  mid(V.B, V.D),  // MBD
  mid(V.C, V.D),  // MCD
  mid(V.C, V.E),  // MCE
];

// Upper half faces: ABC + upper halves of 6 lateral faces
// Upper vertices: A, B, C (x+y+z > 0)
// Each lateral face that has 1 upper vertex → gives trapezoid (upper half)
// Each lateral face that has 2 upper vertices → gives triangle (lower half, so flip)
// Actually:
// Faces with 2 upper vertices (upper halves are trapezia):
//   ABF: A(+), B(+), F(-) → upper part = A,B,H[2],H[1]
//   BCE: B(+), C(+), E(-) → upper part = B,C,H[5],H[2]... wait need correct hex ordering
// Faces with 1 upper vertex:
//   AEF: A(+), E(-), F(-) → upper part = A, H[0], H[1]
//   BDF: B(+), D(-), F(-) → upper part = B, H[3], H[2]
//   CDE: C(+), D(-), E(-) → upper part = C, H[4], H[5]
// Also: face ABC (top face, fully above cut)
//
// Lower half faces: DEF + symmetric lower parts
// By symmetry just negate all coords for lower

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x0d0d1a);
const camera = new THREE.PerspectiveCamera(42, innerWidth/innerHeight, 0.01, 50);
camera.position.set(1.5, 1.3, 1.5);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(devicePixelRatio, 2));
renderer.setSize(innerWidth, innerHeight);
document.body.prepend(renderer.domElement);

const orbit = new OrbitControls(camera, renderer.domElement);
orbit.enableDamping = true; orbit.dampingFactor = 0.07;
orbit.target.set(0,0,0); orbit.update();

scene.add(new THREE.AmbientLight(0x8899cc, 0.6));
const sun = new THREE.DirectionalLight(0xffffff, 1.3);
sun.position.set(2, 3, 2); scene.add(sun);
const fill = new THREE.DirectionalLight(0x4466ff, 0.4);
fill.position.set(-2, 1, -2); scene.add(fill);

function triMesh(p0, p1, p2, color, opacity=0.55) {
  const geo = new THREE.BufferGeometry();
  const pos = new Float32Array([...p0.toArray(),...p1.toArray(),...p2.toArray()]);
  geo.setAttribute('position', new THREE.BufferAttribute(pos,3));
  geo.setIndex([0,1,2]);
  geo.computeVertexNormals();
  return new THREE.Mesh(geo, new THREE.MeshStandardMaterial({
    color, side: THREE.DoubleSide, transparent: true, opacity,
    roughness: 0.35, depthWrite: false
  }));
}

function quadMesh(p0, p1, p2, p3, color, opacity=0.55) {
  const geo = new THREE.BufferGeometry();
  const pos = new Float32Array([...p0.toArray(),...p1.toArray(),...p2.toArray(),...p3.toArray()]);
  geo.setAttribute('position', new THREE.BufferAttribute(pos,3));
  geo.setIndex([0,1,2, 0,2,3]);
  geo.computeVertexNormals();
  return new THREE.Mesh(geo, new THREE.MeshStandardMaterial({
    color, side: THREE.DoubleSide, transparent: true, opacity,
    roughness: 0.35, depthWrite: false
  }));
}

const BLUE = 0x3d8ef0, RED = 0xff6644;

// Upper half meshes (groups so we can move them up)
const upperGroup = new THREE.Group();
upperGroup.add(triMesh(V.A, V.B, V.C, BLUE));        // top face
upperGroup.add(triMesh(V.A, H[0], H[1], BLUE));       // half of AEF (upper triangle)
upperGroup.add(triMesh(V.B, H[2], H[3], BLUE));       // half of BDF
upperGroup.add(triMesh(V.C, H[4], H[5], BLUE));       // half of CDE
upperGroup.add(quadMesh(V.A, V.B, H[2], H[1], BLUE)); // half of ABF (trapezoid: A,B → belt)
upperGroup.add(quadMesh(V.B, V.C, H[5], H[2], BLUE)); // wait, need to check adjacency...
// Let me redo: face BCE: B(+),C(+),E(-). E is connected to B? Let me check: |BE|=√(a²+a²)=1 ✓.
// BCE upper part: B, C, H(CE), H(BE). H[5]=mid(CE), but need mid(BE).
// Hmm wait — I need mid(BE) which isn't in my hex array as defined. Let me reconsider.
// Actually the 6 belt edges are: AE, AF, BD, BF, CD, CE (edges crossing the cut plane).
// H[0]=mid(AE), H[1]=mid(AF), H[2]=mid(BF), H[3]=mid(BD), H[4]=mid(CD), H[5]=mid(CE).
// Face ABF: A(+),B(+),F(-). Upper part: trapezoid A, B, mid(BF)=H[2], mid(AF)=H[1].  ✓
// Face ACE: A(+),C(+),E(-). Upper part: A, C, mid(CE)=H[5], mid(AE)=H[0]. ✓
// Face BCD: B(+),C(+),D(-). Upper part: B, C, mid(CD)=H[4], mid(BD)=H[3]. ✓
// Face AEF: A(+),E(-),F(-). Upper part: triangle A, mid(AE)=H[0], mid(AF)=H[1]. ✓
// Face BDF: B(+),D(-),F(-). Upper part: triangle B, mid(BD)=H[3], mid(BF)=H[2]. ✓
// Face CDE: C(+),D(-),E(-). Upper part: triangle C, mid(CD)=H[4], mid(CE)=H[5]. ✓
scene.remove(...upperGroup.children);
upperGroup.clear();
upperGroup.add(triMesh(V.A, V.B, V.C, BLUE));
upperGroup.add(quadMesh(V.A, V.B, H[2], H[1], BLUE));  // ABF upper
upperGroup.add(quadMesh(V.A, V.C, H[5], H[0], BLUE));  // ACE upper
upperGroup.add(quadMesh(V.B, V.C, H[4], H[3], BLUE));  // BCD upper
upperGroup.add(triMesh(V.A, H[0], H[1], BLUE));         // AEF upper
upperGroup.add(triMesh(V.B, H[3], H[2], BLUE));         // BDF upper
upperGroup.add(triMesh(V.C, H[4], H[5], BLUE));         // CDE upper
scene.add(upperGroup);

// Lower half = mirror (negate all) of upper
const lowerGroup = new THREE.Group();
function neg(v) { return v.clone().negate(); }
lowerGroup.add(triMesh(neg(V.A), neg(V.B), neg(V.C), RED));
lowerGroup.add(quadMesh(neg(V.A), neg(V.B), neg(H[2]), neg(H[1]), RED));
lowerGroup.add(quadMesh(neg(V.A), neg(V.C), neg(H[5]), neg(H[0]), RED));
lowerGroup.add(quadMesh(neg(V.B), neg(V.C), neg(H[4]), neg(H[3]), RED));
lowerGroup.add(triMesh(neg(V.A), neg(H[0]), neg(H[1]), RED));
lowerGroup.add(triMesh(neg(V.B), neg(H[3]), neg(H[2]), RED));
lowerGroup.add(triMesh(neg(V.C), neg(H[4]), neg(H[5]), RED));
scene.add(lowerGroup);

// Hexagon cross-section (gold outline + filled)
const hexPts = [...H, H[0]];
const hexGeo = new THREE.BufferGeometry().setFromPoints(hexPts);
scene.add(new THREE.Line(hexGeo, new THREE.LineBasicMaterial({ color: GOLD, linewidth: 2 })));

// Filled hexagon (fan from H[0])
const hexFillGeo = new THREE.BufferGeometry();
const fillPts = [];
for (let i=1; i<5; i++) { fillPts.push(...H[0].toArray(), ...H[i].toArray(), ...H[i+1].toArray()); }
hexFillGeo.setAttribute('position', new THREE.BufferAttribute(new Float32Array(fillPts.flat()), 3));
hexFillGeo.computeVertexNormals();
const hexFill = new THREE.Mesh(hexFillGeo, new THREE.MeshBasicMaterial({
  color: GOLD, transparent: true, opacity: 0.18, side: THREE.DoubleSide, depthWrite: false
}));
scene.add(hexFill);

// Separation slider
const SEP = 0.6; // max separation distance
document.getElementById('sl').addEventListener('input', e => {
  const t = e.target.value / 100;
  upperGroup.position.y = t * SEP;
  lowerGroup.position.y = -t * SEP;
  hexFill.material.opacity = 0.18 + t * 0.25;
});

function animate() { requestAnimationFrame(animate); orbit.update(); renderer.render(scene, camera); }
animate();

window.addEventListener('resize', () => {
  camera.aspect = innerWidth/innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(innerWidth, innerHeight);
});
window.parent.postMessage({ iframeHeight: 490 }, '*');
</script></body></html>
```the two faces is a hexagon.


