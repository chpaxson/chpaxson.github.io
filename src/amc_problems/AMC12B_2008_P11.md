<!-- metadata -->
2008 AMC12B
Problem 11
answer = 3


# Problem
A cone-shaped mountain has its apex at the top and its base at sea level. The angle between the slope and the base is 
45 degrees. A straight tunnel is dug through the mountain, going from sea level to sea level, with the tunnel at its 
deepest point being 20 miles below the apex. What is the length of the tunnel in miles?

answers = $10\sqrt{2}$, $20\sqrt{2}$, $40$, $20\sqrt{5}$, $40\sqrt{2}$

# Solution

Since the slope makes a **45°** angle with the base, we have $\tan 45° = H/R = 1$, so $H = R$.

The tunnel is a straight chord of the circular base (both endpoints at sea level). At any point on the chord, the depth below the mountain surface equals the mountain's height at that point, which is greatest at the **center** of the base — where the mountain reaches the full height $H$.

For the deepest point of the tunnel to be 20 miles below the apex, the mountain surface directly above it must be at the full apex height $H$. This only happens at the center, so the tunnel must pass **through the center of the base** — i.e., it is a diameter.

$$H = 20 \implies R = 20 \implies \text{tunnel length} = 2R = \boxed{40}$$

The answer is $\textbf{(C)}\ 40$.

```interactive-html
<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0d0d1a;overflow:hidden;font-family:system-ui,sans-serif}
canvas{display:block}
#hint{position:absolute;top:12px;left:12px;color:#445;font-size:11px;line-height:1.9}
#legend{
  position:absolute;top:12px;right:12px;
  background:rgba(10,10,30,.88);border:1px solid rgba(120,140,255,.25);
  border-radius:10px;padding:10px 16px;font-size:12px;line-height:2.1;color:#aac;min-width:180px
}
</style></head><body>
<div id="hint">Drag to orbit · Scroll to zoom</div>
<div id="legend">
  <b style="color:#ccd">Cone Mountain</b><br>
  H = R = 20 mi<br>
  Slope angle = 45°<br>
  <span style="color:#ffcc44">━</span> Tunnel length = <b style="color:#ffcc44">40 mi</b><br>
  <span style="color:#ff6644">━</span> Height H = 20 mi<br>
  <span style="color:#66ddaa">━</span> Radius R = 20 mi
</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js","three/addons/":"https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const H = 20, R = 20, N = 64;

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x0d0d1a);
const camera = new THREE.PerspectiveCamera(42, innerWidth/innerHeight, 0.1, 500);
camera.position.set(48, 38, 52);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(devicePixelRatio, 2));
renderer.setSize(innerWidth, innerHeight);
document.body.prepend(renderer.domElement);

const orbit = new OrbitControls(camera, renderer.domElement);
orbit.enableDamping = true; orbit.dampingFactor = 0.07;
orbit.target.set(0, H/2, 0); orbit.update();

scene.add(new THREE.AmbientLight(0x8899cc, 0.55));
const sun = new THREE.DirectionalLight(0xffffff, 1.4);
sun.position.set(30, 50, 40); scene.add(sun);
const fill = new THREE.DirectionalLight(0x4466ff, 0.35);
fill.position.set(-20, 10, -20); scene.add(fill);

// Cone surface (transparent)
const coneGeo = new THREE.ConeGeometry(R, H, N, 1, true);
const coneMesh = new THREE.Mesh(coneGeo, new THREE.MeshStandardMaterial({
  color: 0x3a6ea8, transparent: true, opacity: 0.28, side: THREE.DoubleSide,
  roughness: 0.4, metalness: 0.0, depthWrite: false
}));
coneMesh.position.set(0, H/2, 0);
scene.add(coneMesh);

// Base disk
const baseGeo = new THREE.CircleGeometry(R, N);
const baseMesh = new THREE.Mesh(baseGeo, new THREE.MeshStandardMaterial({
  color: 0x2255aa, transparent: true, opacity: 0.18, side: THREE.DoubleSide
}));
baseMesh.rotation.x = -Math.PI/2;
scene.add(baseMesh);

// Wireframe edges: base circle and slant lines
function addLine(pts, color, dashed=false) {
  const geo = new THREE.BufferGeometry().setFromPoints(pts);
  const mat = dashed
    ? new THREE.LineDashedMaterial({ color, dashSize: 1.5, gapSize: 1.2, linewidth: 1 })
    : new THREE.LineBasicMaterial({ color });
  const line = new THREE.Line(geo, mat);
  if (dashed) line.computeLineDistances();
  scene.add(line);
}

// Base circle
const circPts = [];
for (let i=0; i<=N; i++) {
  const a = (i/N)*Math.PI*2;
  circPts.push(new THREE.Vector3(R*Math.cos(a), 0, R*Math.sin(a)));
}
addLine(circPts, 0x334488);

// A few slant lines for context
for (let i=0; i<8; i++) {
  const a = (i/8)*Math.PI*2;
  addLine([new THREE.Vector3(R*Math.cos(a),0,R*Math.sin(a)), new THREE.Vector3(0,H,0)], 0x334488);
}

// Tunnel (diameter along x-axis, gold)
addLine([new THREE.Vector3(-R,0,0), new THREE.Vector3(R,0,0)], 0xffcc44);

// Height arrow (red vertical line at center)
addLine([new THREE.Vector3(0,0,0), new THREE.Vector3(0,H,0)], 0xff6644);

// Radius arrow (green horizontal)
addLine([new THREE.Vector3(0,0,0), new THREE.Vector3(R,0,0)], 0x66ddaa);

// 45° angle arc indicator at base edge
const arcPts = [];
for (let i=0; i<=20; i++) {
  const a = (i/20)*(Math.PI/4);  // 0 to 45°
  arcPts.push(new THREE.Vector3(R - 4*Math.cos(a), 4*Math.sin(a), 0));
}
addLine(arcPts, 0xffffff);

// Apex dot
const apexDot = new THREE.Mesh(
  new THREE.SphereGeometry(0.5, 12, 8),
  new THREE.MeshStandardMaterial({ color: 0xff6644, roughness: 0.3 })
);
apexDot.position.set(0, H, 0);
scene.add(apexDot);

// Tunnel endpoint dots
[-R, R].forEach(x => {
  const m = new THREE.Mesh(
    new THREE.SphereGeometry(0.55, 12, 8),
    new THREE.MeshStandardMaterial({ color: 0xffcc44, roughness: 0.3 })
  );
  m.position.set(x, 0, 0);
  scene.add(m);
});

// Center dot
const cDot = new THREE.Mesh(
  new THREE.SphereGeometry(0.4, 12, 8),
  new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.3 })
);
cDot.position.set(0, 0, 0);
scene.add(cDot);

function animate() { requestAnimationFrame(animate); orbit.update(); renderer.render(scene, camera); }
animate();

window.addEventListener('resize', () => {
  camera.aspect = innerWidth/innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(innerWidth, innerHeight);
});
window.parent.postMessage({ iframeHeight: 480 }, '*');
</script></body></html>
```


