<!-- metadata -->
2004 AMC12A
Problem 22
answer = 2


# Problem
Three mutually tangent spheres of radius 1  rest on a horizontal plane.  A sphere of radius 2 rests on them.  What is the distance from the plane to the top of the larger sphere?

answers = $3+\frac{\sqrt{30}}{2}$, $3+\frac{\sqrt{69}}{3}$, $3+\frac{\sqrt{123}}{4}$, $\frac{52}{9}$, $3+2\sqrt{2}$


# Solution

```interactive-html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { background: #1a1a2e; overflow: hidden; font-family: sans-serif; }
  canvas { display: block; }
  #labels { position: absolute; top: 0; left: 0; pointer-events: none; }
  .lbl {
    position: absolute;
    font-size: 13px;
    font-weight: 600;
    background: rgba(0,0,0,0.6);
    padding: 2px 7px;
    border-radius: 4px;
    transform: translate(-50%, -50%);
    white-space: nowrap;
  }
  #hint {
    position: absolute; bottom: 10px; left: 50%; transform: translateX(-50%);
    color: rgba(255,255,255,0.4); font-size: 12px; pointer-events: none;
  }
</style>
</head>
<body>
<div id="labels"></div>
<div id="hint">Drag to rotate · Scroll to zoom</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.163.0/build/three.module.js",
            "three/addons/":"https://cdn.jsdelivr.net/npm/three@0.163.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

// ── Renderer ─────────────────────────────────────────────────────────────────
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.setPixelRatio(devicePixelRatio);
document.body.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1a2e);

// Y-up convention (Three.js default): height = Y axis
const camera = new THREE.PerspectiveCamera(42, window.innerWidth / window.innerHeight, 0.1, 200);
camera.position.set(8, 10, 8);
camera.lookAt(0, 4, 0);

const controls = new OrbitControls(camera, renderer.domElement);
controls.target.set(0, 4, 0);
controls.enableDamping = true;

scene.add(new THREE.AmbientLight(0xffffff, 0.6));
const sun = new THREE.DirectionalLight(0xffffff, 0.9);
sun.position.set(6, 10, 6);
scene.add(sun);

// ── Key geometry (Y = up) ────────────────────────────────────────────────────
const r1 = 1;
const r2 = 2;
const R = 2 / Math.sqrt(3);                          // circumradius of small-center triangle
const smallY = r1;                                    // small sphere centers at y=1
const largeY = smallY + Math.sqrt(9 - R * R);        // = 1 + sqrt(23/3)

// ── Ground plane ──────────────────────────────────────────────────────────────
const planeMesh = new THREE.Mesh(
  new THREE.CircleGeometry(5.5, 64),
  new THREE.MeshStandardMaterial({ color: 0x2a3a4a, side: THREE.DoubleSide, transparent: true, opacity: 0.7 })
);
planeMesh.rotation.x = -Math.PI / 2;
scene.add(planeMesh);
scene.add(new THREE.GridHelper(10, 20, 0x334455, 0x334455));

// ── Sphere helper ─────────────────────────────────────────────────────────────
function makeSphere(radius, color, x, y, z, opacity = 0.72) {
  const mesh = new THREE.Mesh(
    new THREE.SphereGeometry(radius, 48, 48),
    new THREE.MeshStandardMaterial({ color, transparent: true, opacity, roughness: 0.3, metalness: 0.1 })
  );
  mesh.position.set(x, y, z);
  scene.add(mesh);
  return mesh;
}

// ── Three small spheres ───────────────────────────────────────────────────────
const smallCenters = [0, 1, 2].map(i => {
  const a = (i * 2 * Math.PI) / 3;
  return new THREE.Vector3(R * Math.cos(a), smallY, R * Math.sin(a));
});
smallCenters.forEach(c => makeSphere(r1, 0x5b9cf6, c.x, c.y, c.z));

// ── Large sphere ──────────────────────────────────────────────────────────────
const largeCenter = new THREE.Vector3(0, largeY, 0);
makeSphere(r2, 0xf39c12, 0, largeY, 0, 0.7);

// ── Dashed line ───────────────────────────────────────────────────────────────
function dashedLine(a, b, color, opacity = 0.9) {
  const geo = new THREE.BufferGeometry().setFromPoints([a, b]);
  const line = new THREE.Line(geo, new THREE.LineDashedMaterial({
    color, dashSize: 0.15, gapSize: 0.1, transparent: true, opacity, depthTest: false,
  }));
  line.computeLineDistances();
  scene.add(line);
}

const sc0 = smallCenters[0];
const sc1 = smallCenters[1];
const centroidSmall = new THREE.Vector3(0, smallY, 0);
const groundAtSc0   = new THREE.Vector3(sc0.x, 0, sc0.z);
const topOfLarge    = new THREE.Vector3(0, largeY + r2, 0);

// r1 = 1: ground up to small center
dashedLine(groundAtSc0, sc0, 0xe05c5c);
// side = 2: between two small centers
dashedLine(sc0, sc1, 0x5b9cf6);
// circumradius R: centroid to small center (at height smallY)
dashedLine(centroidSmall, sc0, 0x58d68d);
// center-to-center = 3: large → small[0]
dashedLine(largeCenter, sc0, 0xf39c12);
// vertical leg: centroid at smallY up to large center
dashedLine(centroidSmall, largeCenter, 0xc39bd3);
// r2: large center up to top of large sphere
dashedLine(largeCenter, topOfLarge, 0xff9ff3);
// faint vertical axis
dashedLine(new THREE.Vector3(0,0,0), topOfLarge, 0xffffff, 0.2);

// ── Labels ────────────────────────────────────────────────────────────────────
const labelsDiv = document.getElementById('labels');
const labelDefs = [];

function addLabel(text, color, pos) {
  const el = document.createElement('div');
  el.className = 'lbl';
  el.style.color = color;
  el.textContent = text;
  labelsDiv.appendChild(el);
  labelDefs.push({ el, pos });
}

addLabel('r₁ = 1', '#e05c5c',
  new THREE.Vector3(sc0.x + 0.35, 0.5, sc0.z));
addLabel('2r₁ = 2', '#5b9cf6',
  new THREE.Vector3().addVectors(sc0, sc1).multiplyScalar(0.5).add(new THREE.Vector3(0, 0.3, 0)));
addLabel('R = 2/√3', '#58d68d',
  new THREE.Vector3().addVectors(centroidSmall, sc0).multiplyScalar(0.5).add(new THREE.Vector3(0, 0.3, 0)));
addLabel('r₁+r₂ = 3', '#f39c12',
  new THREE.Vector3().addVectors(largeCenter, sc0).multiplyScalar(0.5).add(new THREE.Vector3(0.3, 0, 0)));
addLabel('√69/3', '#c39bd3',
  new THREE.Vector3(0.4, (smallY + largeY) / 2, 0));
addLabel('r₂ = 2', '#ff9ff3',
  new THREE.Vector3(0.4, largeY + r2 / 2, 0));
addLabel('h = 3 + √69/3', '#ffffff',
  new THREE.Vector3(0.5, largeY + r2 + 0.35, 0));

// ── Project & animate ─────────────────────────────────────────────────────────
const tmp = new THREE.Vector3();
function project(p) {
  tmp.copy(p).project(camera);
  return {
    x: (tmp.x * 0.5 + 0.5) * renderer.domElement.clientWidth,
    y: (-tmp.y * 0.5 + 0.5) * renderer.domElement.clientHeight,
  };
}

function animate() {
  requestAnimationFrame(animate);
  controls.update();
  labelDefs.forEach(({ el, pos }) => {
    const p = project(pos);
    el.style.left = p.x + 'px';
    el.style.top  = p.y + 'px';
  });
  renderer.render(scene, camera);
}
animate();

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

window.parent.postMessage({ iframeHeight: 520 }, '*');
</script>
</body>
</html>
```

**Setting up coordinates.** The three unit spheres rest on the plane, so their centers are at height $z = 1$. Since they are mutually tangent, the distance between any two centers is $1 + 1 = 2$, so the centers form an equilateral triangle with side length $2$. The circumradius of this triangle is

$$R = \frac{2}{\sqrt{3}}$$

**Finding the large sphere's center.** By symmetry the large sphere's center is directly above the centroid of the triangle. Its center-to-center distance to each small sphere is $1 + 2 = 3$. Applying the Pythagorean theorem with the horizontal leg $R$ and vertical leg $(h - 1)$:

$$R^2 + (h-1)^2 = 3^2 \implies \frac{4}{3} + (h-1)^2 = 9 \implies (h-1)^2 = \frac{23}{3}$$

$$h = 1 + \sqrt{\frac{23}{3}} = 1 + \frac{\sqrt{69}}{3}$$

**Distance from plane to top.** The top of the large sphere is $h + r_2$ above the plane:

$$\text{height} = 1 + \frac{\sqrt{69}}{3} + 2 = \boxed{3 + \frac{\sqrt{69}}{3}}$$