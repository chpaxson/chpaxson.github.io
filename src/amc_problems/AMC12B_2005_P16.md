<!-- metadata -->
2005 AMC12B
Problem 16
answer = 3


# Problem
Eight spheres of radius 1 are placed in the corners of a cube of side length 2, each tangent to the three faces meeting at 
its corner. A ninth sphere is placed at the center of the cube. What is the radius of the ninth sphere?

answers = $1$, $\sqrt{2}-1$, $\sqrt{3}-1$, $\frac{\sqrt{3}}{2}$, $2-\sqrt{2}$

# Solution

Place the cube with its center at the origin, and the 8 corner sphere centers at $(\pm 1, \pm 1, \pm 1)$. Because each corner sphere (radius 1) is tangent to the three adjacent cube faces, each face is exactly 1 unit from its nearest sphere center, so the cube's faces lie at $x = \pm 2$, $y = \pm 2$, $z = \pm 2$ — a cube of side 4 (equivalently, side 2 measured in sphere-diameter units).

**Distance from the cube center to any corner sphere center:**

$$d = \sqrt{1^2 + 1^2 + 1^2} = \sqrt{3}$$

**Radius of the ninth sphere.** The ninth sphere sits at the origin and is externally tangent to each of the 8 corner spheres. For two externally tangent spheres, the distance between centers equals the sum of radii:

$$r + 1 = \sqrt{3} \implies \boxed{r = \sqrt{3} - 1}$$

The answer is $\textbf{(C)}\ \sqrt{3}-1$.

```interactive-html
<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"/><style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0d0d1a;overflow:hidden;font-family:system-ui,sans-serif}
canvas{display:block}
#hint{position:absolute;top:12px;left:12px;color:#445;font-size:11px;line-height:1.9}
#legend{
  position:absolute;top:12px;right:12px;
  background:rgba(10,10,30,.88);border:1px solid rgba(120,140,255,.25);
  border-radius:10px;padding:10px 16px;font-size:12px;line-height:2.2;color:#aac
}
#ui{
  position:absolute;bottom:16px;left:50%;transform:translateX(-50%);
  background:rgba(10,10,30,.92);border:1px solid rgba(120,140,255,.3);
  border-radius:12px;padding:10px 24px;display:flex;align-items:center;gap:14px
}
#ui label{color:#99a;font-size:12px;white-space:nowrap}
input[type=range]{
  flex:1;-webkit-appearance:none;height:4px;border-radius:2px;width:200px;
  background:linear-gradient(to right,#4a9eff,#9b6bff);cursor:pointer;outline:none
}
input[type=range]::-webkit-slider-thumb{
  -webkit-appearance:none;width:16px;height:16px;border-radius:50%;
  background:#fff;cursor:pointer;box-shadow:0 0 6px rgba(74,158,255,.7)
}
</style></head><body>
<div id="hint">Drag to orbit · Scroll to zoom</div>
<div id="legend">
  <b style="color:#ccd">Nine-Sphere Cube, radius = 1</b><br>
  <span style="color:#4a9eff">●</span> Corner spheres (8)<br>
  <span style="color:#ffcc44">●</span> Central sphere, r = √3−1<br>
  <span style="color:#ff6644">━</span> Distance = √3<br>
  Cube side = 4 (scaled)
</div>
<div id="ui">
  <label>Solid</label>
  <input type="range" id="sl" min="0" max="100" value="40"/>
  <label>Wireframe</label>
</div>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js","three/addons/":"https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"}}
</script>
<script type="module">
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

// Work in unit-normalised coords: cube [-1,1]^3, sphere radius = 0.5, center sphere r = (√3-1)/2
const S = 1;        // half-side
const R = 0.5;      // corner sphere radius (scaled: 1/2 of half-side)
const RC = (Math.sqrt(3)-1) * R; // central sphere radius

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x0d0d1a);
const camera = new THREE.PerspectiveCamera(42, innerWidth/innerHeight, 0.01, 50);
camera.position.set(3.5, 2.8, 3.5);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(devicePixelRatio,2));
renderer.setSize(innerWidth, innerHeight);
document.body.prepend(renderer.domElement);

const orbit = new OrbitControls(camera, renderer.domElement);
orbit.enableDamping = true; orbit.dampingFactor = 0.07;
orbit.target.set(0,0,0); orbit.update();

scene.add(new THREE.AmbientLight(0x8899cc, 0.55));
const sun = new THREE.DirectionalLight(0xffffff, 1.3);
sun.position.set(4,6,4); scene.add(sun);
const fill = new THREE.DirectionalLight(0x4466ff, 0.4);
fill.position.set(-3,1,-3); scene.add(fill);

// Cube wireframe
const cubeGeo = new THREE.BoxGeometry(2*S,2*S,2*S);
const cubeEdges = new THREE.LineSegments(
  new THREE.EdgesGeometry(cubeGeo),
  new THREE.LineBasicMaterial({ color: 0x2a3a5a, transparent:true, opacity:0.6 })
);
scene.add(cubeEdges);

// Corner sphere positions: all 8 corners (±(S-R), ±(S-R), ±(S-R))
const signs = [-1,1];
const cornerMeshes = [];
for(const sx of signs) for(const sy of signs) for(const sz of signs) {
  const pos = new THREE.Vector3(sx*(S-R), sy*(S-R), sz*(S-R));
  const mat = new THREE.MeshStandardMaterial({
    color: 0x3d8ef0, roughness:0.3, metalness:0.05,
    transparent:true, opacity:0.75
  });
  const m = new THREE.Mesh(new THREE.SphereGeometry(R,32,20), mat);
  m.position.copy(pos);
  scene.add(m);
  cornerMeshes.push(m);
}

// Central sphere
const centerMat = new THREE.MeshStandardMaterial({
  color: 0xffcc44, roughness:0.25, metalness:0.1,
  transparent:true, opacity:0.9
});
const centerMesh = new THREE.Mesh(new THREE.SphereGeometry(RC,32,20), centerMat);
scene.add(centerMesh);

// Distance line from center to one corner sphere center (illustrate √3)
const csc = new THREE.Vector3(S-R, S-R, S-R); // closest upper-right-front corner center
const lineGeo = new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(0,0,0), csc]);
const distLine = new THREE.Line(lineGeo, new THREE.LineBasicMaterial({color:0xff6644, linewidth:2}));
scene.add(distLine);

// Label at midpoint of distance line
function makeLabel(text, pos, color='#ccddff') {
  const c=document.createElement('canvas'); c.width=120; c.height=40;
  const ctx=c.getContext('2d');
  ctx.font='bold 18px system-ui'; ctx.fillStyle=color;
  ctx.textAlign='center'; ctx.textBaseline='middle';
  ctx.fillText(text,60,20);
  const sp=new THREE.Sprite(new THREE.SpriteMaterial({
    map:new THREE.CanvasTexture(c),transparent:true,depthTest:false
  }));
  sp.scale.set(0.55,0.18,1);
  sp.position.copy(pos);
  scene.add(sp);
}
makeLabel('√3', csc.clone().multiplyScalar(0.55).add(new THREE.Vector3(0.15,0.1,0)), '#ff9966');
makeLabel('r = √3−1', new THREE.Vector3(0.55, 0.2, 0), '#ffcc44');

// Transparency slider
document.getElementById('sl').addEventListener('input', e => {
  const t = e.target.value / 100;
  const op = 0.9 - t * 0.75;
  cornerMeshes.forEach(m => m.material.opacity = op);
  centerMat.opacity = Math.max(0.15, 0.9 - t * 0.5);
});

function animate() { requestAnimationFrame(animate); orbit.update(); renderer.render(scene, camera); }
animate();

window.addEventListener('resize', () => {
  camera.aspect = innerWidth/innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(innerWidth, innerHeight);
});
window.parent.postMessage({ iframeHeight: 480 }, '*');
</script></body></html>
```


